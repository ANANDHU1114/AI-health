apiVersion: v1
kind: Service
metadata:
  name: ai-symptom-checker-service
spec:
  type: LoadBalancer
  selector:
    app: ai-symptom-checker
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
